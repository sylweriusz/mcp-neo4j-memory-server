{
  "mcpServers": {
    "graph-memory": {
      "command": "docker",
      "args": [
        "run",
        "-i",
        "--rm",
        "mcp-neo4j-memory"
      ],
      "env": {
        "NEO4J_URI": "bolt://localhost:7687",
        "NEO4J_USERNAME": "neo4j",
        "NEO4J_PASSWORD": "password",
        "NEO4J_DATABASE": "neo4j"
      }
    }
  }
}
